using UnityEngine;

namespace DuckTunes.Systems.Interaction
{
    public class SelectWithTag : MonoBehaviour//, ISelector
    {
        //[SerializeField] private string _selectableTag = "Selectable";
        //private Transform[] _selection;
        //private Camera _cam;

        //public Transform[] GetSelection()
        //{
        //    return _selection;
        //}

        //public void Check(Ray ray)
        //{
        //    _selection = null;
        //    if (_cam == null) { _cam = Camera.main; }
        //    var hit = Physics2D.Raycast(ray.origin, ray.direction, _cam.farClipPlane);
        //    if (hit.collider != null)
        //    {
        //        var selection = hit.transform;
        //        if (selection.CompareTag(_selectableTag))
        //        {
        //            //
        //        }
        //    }
        //}
    }

}